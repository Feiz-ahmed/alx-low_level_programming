#include "main.h"
/**
 *_strlen_recursion - measure the length of the string
 *@s :string to be measured
 *Return:thelengthofs 
 */
int _strlen_recursion(char *s)
{
int len = 0;
if (*s)
{
len++;
len += _strlen_recursion(s + 1);
}
return (len);
}
