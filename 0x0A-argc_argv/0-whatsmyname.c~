#include "main.h"
#include <stdio.h>
/**
 *main- includes the function
 *@argc- counts the total number of arguments
 *@argv[]-contains the total number of arguments.
 *Return: 0
 */
int main(int argc, char *argv[])
{
argc = 0;
printf("%s\n", argv[argc]);
return (0);
}
